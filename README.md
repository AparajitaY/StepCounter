# StepCounter
Contains Verilog HDL source code for a FPGA based embedded system that works as a pedometer. Includes modules for each function and uses a Finite State Machine (FSM) architecture. The design was implemented successfully on a FPGA board and real time monitoring using tilt sensor was done.
